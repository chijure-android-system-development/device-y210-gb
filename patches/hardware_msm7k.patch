diff --git a/libcopybit/Android.mk b/libcopybit/Android.mk
index b7f704a..39bcc13 100644
--- a/libcopybit/Android.mk
+++ b/libcopybit/Android.mk
@@ -22,7 +22,7 @@ include $(CLEAR_VARS)
 LOCAL_MODULE_TAGS := optional
 LOCAL_PRELINK_MODULE := false
 LOCAL_MODULE_PATH := $(TARGET_OUT_SHARED_LIBRARIES)/hw
-LOCAL_SHARED_LIBRARIES := liblog
+LOCAL_SHARED_LIBRARIES := liblog libcutils
 LOCAL_SRC_FILES := copybit.cpp
 LOCAL_MODULE := copybit.msm7k
 LOCAL_C_INCLUDES += hardware/msm7k/libgralloc
@@ -34,7 +34,7 @@ ifeq ($(TARGET_BOARD_PLATFORM),qsd8k)
 include $(CLEAR_VARS)
 LOCAL_PRELINK_MODULE := false
 LOCAL_MODULE_PATH := $(TARGET_OUT_SHARED_LIBRARIES)/hw
-LOCAL_SHARED_LIBRARIES := liblog
+LOCAL_SHARED_LIBRARIES := liblog libcutils
 LOCAL_SRC_FILES := copybit.cpp
 LOCAL_MODULE := copybit.qsd8k
 LOCAL_C_INCLUDES += hardware/libhardware/modules/gralloc
diff --git a/libcopybit/copybit.cpp b/libcopybit/copybit.cpp
index ef37a27..23973c0 100644
--- a/libcopybit/copybit.cpp
+++ b/libcopybit/copybit.cpp
@@ -17,7 +17,9 @@
 
 #define LOG_TAG "copybit"
 
+#include <stdlib.h>
 #include <cutils/log.h>
+#include <cutils/properties.h>
 
 #include <linux/msm_mdp.h>
 #include <linux/fb.h>
@@ -389,6 +391,20 @@ static int stretch_copybit(
     struct copybit_context_t* ctx = (struct copybit_context_t*)dev;
     int status = 0;
     if (ctx) {
+        // Workaround: MDP fails blitting the status bar strip (320x25 at y=0),
+        // leaving stale pixels. Return -EINVAL to force SF software compositor.
+        // Controlled by debug.copybit.disable_statusbar (default "1" = active).
+        if (dst->h <= 26) {
+            static int sDisableSB = -1;
+            if (sDisableSB < 0) {
+                char prop[PROPERTY_VALUE_MAX];
+                property_get("debug.copybit.disable_statusbar", prop, "1");
+                sDisableSB = atoi(prop);
+            }
+            if (sDisableSB)
+                return -EINVAL;
+        }
+
         struct {
             uint32_t count;
             struct mdp_blit_req req[12];
